# Your assignments

Run `python tools/start_exercise.py foundations` from the repository root. Edit and
commit the resulting files in your fork. This directory is deliberately excluded
from website publication and canonical example checks. Do not store credentials,
large recordings, virtual environments, or personal information here.
